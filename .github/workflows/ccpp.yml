name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
      name: dependencies
      run: apt install libaio-dev
    - name: cmake 
      run: cmake .
    - name: make
      run: make
    - name: make test
      run: make test

